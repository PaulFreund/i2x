var storeHandler = require('storeHandler');

var store = storeHandler.create({file: 'teststore.db'});
var Step = require('step');
var util = require('util');

store.on('ready', function() {
    
    store.emit('set', 'hi.ho.lets.go', 'lala', function(err) {
        store.emit('get', 'hi.ho', function(data) {
            console.log('get hi: ' + util.inspect( data ));
            
            process.exit();
        });        
    }); 

    
    //process.exit();
});